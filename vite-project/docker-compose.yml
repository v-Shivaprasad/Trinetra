version: "3.9"
services:
  vite-app:
    build: .
    container_name: vite-dev
    network_mode: "host"
    ports:
      - "5173:5173"
    volumes:
      - .:/vite-project
      - /vite-project/node_modules
    command: ["pnpm", "run", "dev", "--", "--host", "0.0.0.0"]
